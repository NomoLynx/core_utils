# Common Infrastructure Library

A shared Rust library providing core infrastructure and reusable components for my compiler and linker ecosystem.

It centralizes common abstractions, intermediate representations, binary format utilities, and experimental code generation ideas that power my evolving systems-level tooling.

## EGraph

A small e-graph helper module for equality-based rewriting and optimization experiments.
